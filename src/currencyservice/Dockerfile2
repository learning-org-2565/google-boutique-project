# # iteration 2 we are building docker file for currencyservice

# FROM node:18 

# WORKDIR /app

# COPY package*.json ./ 

# RUN npm install 

# COPY . .

# EXPOSE 7000

# # Addking health checks for currecyservice
# HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 CMD node -e "console.log('health-checks passed')" || exit 1

# CMD ["node", "server.js"]